SELECT artist_lastfm, MIN(tags), MAX(tags)
FROM hue__tmp_artists LATERAL VIEW explode(split(trim(tags_lastfm),';')) adTable AS tags
where tags not LIKE ""
GROUP BY artist_lastfm
HAVING min(tags) = max(tags)
